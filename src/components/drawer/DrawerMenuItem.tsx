import { View, Text } from "react-native";

type DrawerMenuItemProps = Record<string, never>;

export default function DrawerMenuItem(_props: DrawerMenuItemProps) {
  return (
    <View>
      <Text>Drawer Menu Item</Text>
    </View>
  );
}
