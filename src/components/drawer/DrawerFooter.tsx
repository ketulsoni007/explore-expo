import { View, Text } from "react-native";

type DrawerFooterProps = Record<string, never>;

export default function DrawerFooter(_props: DrawerFooterProps) {
  return (
    <View>
      <Text>Drawer Footer</Text>
    </View>
  );
}
