import { View, Text } from "react-native";

type PartnerListItemProps = Record<string, never>;

export default function PartnerListItem(_props: PartnerListItemProps) {
  return (
    <View>
      <Text>Partner List Item</Text>
    </View>
  );
}
