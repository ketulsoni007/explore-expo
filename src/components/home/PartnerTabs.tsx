import { View, Text } from "react-native";

type PartnerTabsProps = Record<string, never>;

export default function PartnerTabs(_props: PartnerTabsProps) {
  return (
    <View>
      <Text>Partner Tabs</Text>
    </View>
  );
}
