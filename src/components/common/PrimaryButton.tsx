import { View, Text } from "react-native";

type PrimaryButtonProps = Record<string, never>;

export default function PrimaryButton(_props: PrimaryButtonProps) {
  return (
    <View>
      <Text>Primary Button</Text>
    </View>
  );
}
